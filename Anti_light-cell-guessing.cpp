#include <iostream>
#include <string>
#include <math.h>
#include <iomanip>
using namespace std;

int main(){
    int n, m, t;
    int arr[n][m];
    int count = 0;
    cin >> t;
    while(t--){
        cin >> n >> m;
    }
    for(int i = 0; i < n; i++){
        for(int j = 0; j < m; j++){
            count += 
        }
    }
    return 0;
}